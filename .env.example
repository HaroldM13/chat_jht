# ─── Backend ──────────────────────────────────────────────────────────────────
DATABASE_NAME=jht_chat
SECRET_KEY=cambia_esto_por_una_clave_larga_y_aleatoria
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
ALLOWED_ORIGINS=http://localhost:5173

# Twilio (opcional — solo si querés verificación OTP por SMS)
# TWILIO_ACCOUNT_SID=ACxxxx
# TWILIO_AUTH_TOKEN=xxxx
# TWILIO_VERIFY_SID=VAxxxx

# ─── Frontend (baked en el build) ─────────────────────────────────────────────
# Para uso local no hace falta cambiar nada.
# Para producción: reemplazar con la URL pública de tu backend.
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000
